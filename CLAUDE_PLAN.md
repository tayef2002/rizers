# Rizers — Full AI Integration Plan
**Last Updated: 2026-07-07**

---

## Ultimate Vision
Rizers = World's First Islamic AI-Native Productivity App
- 24/7 Agentic AI Partner — শুধু chat না, পুরো app control করে
- Voice দিয়ে সব কিছু — screen touch ছাড়া
- Community-based productive social platform
- Play Store + App Store publish হবে (React Native)
- "Hey Rizer" wake word — iPhone/Android এ

---

---

## AI এর Core Philosophy — সবচেয়ে Important

### 1. "Template না — Thinking"

এটা Rizers AI এর সবচেয়ে গুরুত্বপূর্ণ principle।
বাজারের সব AI template follow করে। Rizers এর AI চিন্তা করে।

```
❌ Template approach:
"User ফজর miss করলে এই message পাঠাও"
→ সবাইকে same message → কাজ করে না

✅ Thinking approach:
"ফজর miss হলে কারণ খোঁজো।
 কারণ বুঝে তারপর সিদ্ধান্ত নাও"
→ প্রতিজনের জন্য আলাদা সিদ্ধান্ত → কাজ করে ✅
```

### 2. "সমস্যা না — মানুষকে জানো"

```
❌ ভুল:
"তোমার সবচেয়ে বড় সমস্যা কী?" জিজ্ঞেস করা
→ শুধু symptom জানা হয়

✅ সঠিক:
দেখে বোঝা — behavior, pattern, timing
→ মানুষটাকে জানা হয়

যা দেখে বোঝে:
• কখন app open করে
• কোন কাজে "হ্যাঁ" বলে, কোনটা এড়ায়
• কতদিন পর ফিরে আসে
• habit miss করলে কী বলে
• রাতে কখন active থাকে
• to-do তে কী লিখে রাখে
```

### 3. "Scratch থেকে শুরু — Friend এর মতো"

```
❌ ভুল approach:
User register করলো →
AI: "এই নাও ৫০টা feature, সব setup করো"
User: overwhelmed → app ছেড়ে দিলো

✅ সঠিক approach:
User register করলো →
AI: "আস্সালামু আলাইকুম, কেমন আছো?"
→ ধীরে ধীরে জানলো
→ ১টা ছোট কাজ দিলো
→ সে করলো → celebrate করলো
→ পরের দিন আরেকটু
→ সে নিজেই চাইতে শুরু করলো
```

**Human Psychology:**
```
Day 1:  ১টা habit → সহজ → করলো ✅
Day 3:  ২টা habit → অভ্যাস হচ্ছে ✅
Day 7:  ৩টা habit → নিজেই চাইলো ✅
Day 14: AI ছাড়া অসম্পূর্ণ লাগে ✅
Day 30: Rizers তার জীবনের অংশ ✅
```

**AI কখনো overwhelm করবে না:**
- Day 1 এ ৫০টা tool দেখাবে না
- User না চাইলে নতুন feature suggest করবে না
- User এর pace এ চলবে, নিজের pace এ না
- ছোট জয় celebrate করবে বড় করে

---

### 4. "Reasoning-First AI" — AI এর Thinking Framework

AI কে template শেখানো হবে না। শেখানো হবে **কিভাবে চিন্তা করতে হয়।**

```
STEP 1 — DATA দেখো
আমার কাছে এই user এর কী কী আছে?
• সে কখন app open করে
• কোন কাজ করে, কোনটা এড়িয়ে যায়
• কখন ঘুমায়, কখন ওঠে
• to-do তে কী লিখে রেখেছে
• কোন habit কতদিন ধরে করছে

STEP 2 — ROOT CAUSE খোঁজো
সমস্যার symptom না, আসল কারণ কী?

"ফজর হচ্ছে না" → কেন?
→ রাতে দেরিতে ঘুমায় → কেন?
→ রাত ১১টায় কাজ করে → কেন?
→ দিনে সময় পায় না → কেন?
→ দিনের schedule এলোমেলো
আসল সমস্যা: ফজর না, দিনের schedule

STEP 3 — STRATEGY বানাও
এই specific মানুষটার জন্য
এই specific কারণের জন্য
কী করলে কাজ হবে?

STEP 4 — PERMISSION নাও
তাকে impose করো না
"এটা করলে কেমন হয়?" জিজ্ঞেস করো
→ "আজকে ৮ গ্লাস পানি পান করবে?
   আমি habit টা create করে দিই?"

STEP 5 — FOLLOW করো
সে করলো কিনা দেখো
না করলে কেন হলো না বোঝো
আবার adjust করো
```

**Real Example — Reasoning in action:**
```
AI এর কাছে data আছে:
• User রাত ১২টায় ঘুমায়
• To-do: "রাত ১০:৩০—১২:০০ → ২টা কাজ"
• ফজর miss ৫ দিন ধরে

AI চিন্তা করলো:
"রাতে কাজ করে → দেরি ঘুম → ফজর miss।
 ফজরের reminder কাজ হবে না।
 রাতের কাজটাই আসল সমস্যা।"

AI বললো:
"ভাই, দেখলাম রাতে ১০:৩০ থেকে কাজ করো।
 এগুলো কি কাল থেকে সকাল ৬-৮টায়
 করা যাবে? রাত খালি রাখলে
 ফজর নিজেই ঠিক হবে।"
```

---

### AI এর Personality — "বন্ধু, AI না"

```
❌ AI এর মতো কথা:
"আপনার productivity score 67% এ আছে।
 আমি recommend করছি habit frequency
 বাড়ানো উচিত।"

✅ বন্ধুর মতো কথা:
"ভাই, এই সপ্তাহে কিন্তু ভালোই করেছো!
 ফজরটা একটু কষ্ট হচ্ছে মনে হচ্ছে —
 কাল একটু আগে ঘুমাবে?"
```

**Tone Rules:**
```
• নাম ধরে ডাকবে (Tayef ভাই / আপু)
• "আমি" না বলে "আমরা" বলবে মাঝে মাঝে
• Mistake এ: "কোনো সমস্যা নেই, কাল হবে"
• Success এ: "মাশাআল্লাহ! অসাধারণ!"
• Struggle এ: "কঠিন লাগছে জানি, কিন্তু..."
• কখনো lecture দেবে না
• কখনো compare করবে না অন্যের সাথে
```

---

### User Journey — Day by Day AI Strategy

```
Day 1 (Registration):
AI শুধু জানবে:
• নাম
• বয়স  
• একটাই সমস্যা — "সবচেয়ে বড় সমস্যা কী?"
→ শুধু সেই একটা নিয়েই কাজ শুরু

Day 2-3:
• কেমন গেলো জিজ্ঞেস করবে
• ১টা small win celebrate করবে
• আস্তে করে ২য় একটা জিনিস suggest করবে

Day 7:
• এক সপ্তাহের mini summary দেবে
• User নিজেই বলবে "আরেকটা habit যোগ করো"

Day 14:
• User এর pattern AI বুঝে গেছে
• Proactive suggestions শুরু হবে

Day 30:
• Full app naturally use হচ্ছে
• User বুঝতেও পারেনি কখন সব setup হলো
```

---

### System Prompt Design — AI এর "Brain"

প্রতিটা conversation এ AI এর কাছে এই data inject হবে:

```
━━━ IDENTITY ━━━
তুমি "Rizers AI" — {userName} এর personal
Muslim productivity partner।
তুমি template follow করো না। তুমি চিন্তা করো।
বন্ধুর মতো কথা বলো। বাংলায়। সংক্ষেপে।

━━━ USER PROFILE ━━━
নাম: {userName}
বয়স: {age}
পেশা: {profession}
Rizers এ: {daysSinceJoined} দিন
কেন এসেছে: {mainReason}
Motivation level: {motivationLevel}
Response style: {responseStyle}
Best active time: {bestActiveTime}

━━━ DAILY DATA ━━━
ঘুম: {lastNightSleep} (goal: {sleepGoal})
ঘুমানোর সময়: {sleepTime}
ওঠার সময়: {wakeTime}
নামাজ আজ: {namazToday}
Habits আজ: {habitsToday}
Pending tasks: {pendingTasks}
To-do তে যা লিখেছে: {todoList}

━━━ 7-DAY HISTORY ━━━
{weeklyData}
Pattern: {detectedPattern}

━━━ ACTIVE SETUP ━━━
Active habits: {activeHabits}
Current streak: {currentStreak} দিন
Goals: {goals}
Features on: {activeFeatures}

━━━ THINKING RULES ━━━
১. সমস্যা দেখলে root cause খোঁজো
২. Data দেখে pattern বোঝো
৩. এই specific user এর জন্য ভাবো
৪. Permission নিয়ে কাজ করো
৫. Day {daysSinceJoined} অনুযায়ী
   কতটুকু suggest করবে ঠিক করো
৬. ২-৩ লাইনে বলো, বেশি না
৭. Judge করো না, celebrate করো
৮. AI এর মতো কথা বলো না

━━━ TOOLS ━━━
[সব tool list]
```

---

### কখন কোন Feature Introduce করবে AI:

```
Day 1-3:    শুধু ১টা main problem নিয়ে কাজ
Day 4-7:    ২য় habit suggest করবে
Day 8-14:   Sleep tracker introduce করবে
Day 15-21:  Quran tracker suggest করবে
Day 22-30:  Full namaz tracking শুরু
Day 30+:    সব feature naturally active
            User নিজেই explore করছে
```

---

## AI System — 5 Core Features

### 1. Floating AI Widget (Bottom Right)
- সব page এ fixed position, bottom-right corner
- Circular orange button
- Icon এর মাঝে **remaining credit number** দেখাবে
- Credit state অনুযায়ী color change:
  - 🟠 21-30 → Orange (full)
  - 🟡 11-20 → Yellow (half)
  - 🔴 1-10  → Red (low)
  - ⚫ 0     → Grey (locked)
- Click → chat drawer slides up from bottom

### 2. Credit System
- **30 Credit/day** per user
- **1 Credit = 1 full exchange** (user message + AI reply মিলে)
- AI reply আলাদা credit নেয় না — পুরো exchange = 1 credit
- Midnight এ reset হয়
- 0 credit হলে chat lock, "কাল আবার পাবে" message

### 3. Agentic Onboarding Flow

#### Phase A — Data Collection (Registration এর পরেই)

AI আগে যা আগে থেকে জানে (Profile থেকে):
```
• নাম
• পেশা (ছাত্র / চাকরি / ব্যবসা)
• About section এ যা দিয়েছে
• বয়স
• কোথায় থাকে
```

AI তারপর conversation এ জানবে:
```
১. Rizers এ কেন এসেছো?
   [নামাজ ঠিক করতে] [ঘুম ঠিক করতে]
   [Productive হতে] [অন্য কারণে]

২. সকালে কখন ওঠো?
   [৫টার আগে] [৫-৭টা] [৭-৯টা] [৯টার পরে]

৩. রাতে কখন ঘুমাও?
   [১০টার আগে] [১০-১২টা] [১২-২টা] [২টার পরে]

৪. সবচেয়ে কষ্টের কোনটা?
   [ফজর miss হয়] [ঘুম ঠিক নেই]
   [কোরআন হয় না] [কিছুই গুছিয়ে করতে পারি না]

৫. কতটা busy সাধারণত?
   [খুব busy] [মোটামুটি] [সময় আছে]
```

**UI Pattern:**
- প্রতিটা question এ 4-5টা **Chip/Button** (suggested answers)
- User chip select করবে অথবা নিজে type করবে
- এক প্রশ্ন শেষ হলে পরের প্রশ্ন আসবে — সব একসাথে না

#### Phase B — First Setup (Permission নিয়ে, ২-৩টা মাত্র)

সব data পেয়ে AI **২-৩টা মাত্র** কাজ করবে, ৫০টা না:
```
AI: "ঠিক আছে! চলো শুরু করি।
     আজকে একটাই কাজ —
     তুমি কি দিনে ৮ গ্লাস পানি পান করবে?
     আমি এই habit টা তোমার জন্য
     create করে দিই?"

[হ্যাঁ, করবো ✅] [এখন না]

User: "হ্যাঁ"

AI: [createHabit() call করলো]
    "Done! আর একটা —
     রাত ১১টায় একটা reminder দিই
     ঘুমানোর কথা মনে করিয়ে দেবে?"
```

**First day এ maximum ২-৩টা setup। বাকি সব পরে।**

#### Phase C — Invisible Profile Building

AI চুপচাপ দেখতে থাকে এবং profile তৈরি করে:
```
দুইজন user, দুইজনেই বললো "ফজর পড়তে পারি না"

User A:
• রাত ১১টায় app open করে
• "হ্যাঁ চেষ্টা করবো" বলে
• পরদিন check-in দেয়
• ভাষা: উৎসাহী
AI বুঝলো → motivated, শুধু reminder দরকার

User B:
• রাত ২টায় app open করে
• "দেখি" বলে এড়িয়ে যায়
• ৩ দিন পর আসে
• ভাষা: ক্লান্ত
AI বুঝলো → struggling, আগে বিশ্বাস দরকার
            reminder এখন কাজ করবে না
```

**Invisible Profile তৈরি হয় — সরাসরি জিজ্ঞেস না করেই:**
```
• Motivation level: high / medium / low
• Response style: quick / slow / avoidant
• Best active time: morning / evening / night
• Accountability need: high / low
• Learning style: guided / self-directed
```

### 4. Tool Calling — AI এর App Control
AI এর কাছে এই "tools" থাকবে যা সে call করতে পারবে:

```
HABIT TOOLS:
• createHabit(name, icon, frequency, reminder)
• deleteHabit(id)
• updateHabit(id, changes)

NAMAZ TOOLS:
• setNamazReminder(prayer, time)
• markNamaz(prayer, date, status)

SLEEP TOOLS:
• setSleepGoal(hours)
• logSleep(bedtime, wakeTime, quality)

QURAN TOOLS:
• setQuranGoal(surah, ayat)
• markQuranRead(surah, ayat)

TASK/TODO TOOLS:
• createTask(title, dueDate, estimatedTime, priority)
• createRoutine(name, tasks[])
• scheduleTasksBeforeDeadline(tasks[], deadline)
• setTaskReminder(taskId, reminderTime)
• learnUserTaskDuration(taskType, actualTime)

PROFILE TOOLS:
• setupFullProfile(goals, lifestyle, preferences)
• resetApp()

READ/ANALYSIS TOOLS:
• getUserStats()
• getWeeklyReport()
• getSleepHistory(days)
• getNamazHistory(days)
```

### 5. Smart Task Scheduling — "Voice থেকে Time Block"

User voice বা chat এ deadline বললে AI নিজে সব schedule করে reminder set করে দেবে।

**Example Flow:**
```
User (সকাল ৯টায়):
"আজ দুপুরের মধ্যে ৩টা কাজ শেষ করতে চাই"

AI জিজ্ঞেস করলো: "কাজগুলো কী?"

User: "Assignment লেখা, email পাঠানো, room গোছানো"

AI চিন্তা করলো:
• এখন ৯:০০ AM, deadline ১২:০০ PM
• হাতে ৩ ঘণ্টা
• কঠিন কাজ আগে (Assignment)
• নামাজের সময় buffer রাখো

AI create করলো:
✅ ৯:১৫ AM — Assignment (১ ঘণ্টা) → reminder ৯:১০ এ
✅ ১০:১৫ AM — Email (৩০ মিনিট) → reminder ১০:১০ এ
✅ ১০:৪৫ AM — Room (৪৫ মিনিট) → reminder ১০:৪০ এ
🏁 সব শেষ: ১১:৩০ AM ✅
```

**AI যা দেখে Schedule করে:**
```
• Current time
• Deadline
• Task complexity (কঠিন আগে)
• User এর past task duration (শিখে নেয়)
• নামাজের ওয়াক্ত (buffer রাখে)
• Tasks এর মাঝে ৫-১০ মিনিট বিরতি
```

**Self-Learning:**
```
Week 1: AI ধরলো Assignment = ১ ঘণ্টা
        User নিলো ১.৫ ঘণ্টা

Week 2: AI automatically ১.৫ ঘণ্টা দেবে ✅
        User এর pattern শিখে নেয়
```

---

### 6. Proactive AI — "Rizers Pulse"
App open হলে AI চুপচাপ গত **7 দিনের history** check করবে।
দরকার মনে করলে widget pulse করবে + bubble দেখাবে।

**AI কথা বলবে যখন:**
- ৩+ দিন কোনো নামাজ miss
- Habit streak ভাঙতে চলেছে (আজ কিছু করা হয়নি)
- ঘুম consistently কম (৫ দিন goal miss)
- Quran ৪+ দিন বন্ধ
- নতুন user (onboarding বাকি)

**AI চুপ থাকবে যখন:**
- আজ আগে already notification দিয়েছে
- User নিজে এইমাত্র chat করেছে
- রাত ১২টার পর (ঘুমানোর সময়)
- সব metrics ভালো
- User "বিরক্ত করো না" বলেছে

### 6. Daily Summary (Morning + Night)
**সকাল (Fajr time এর পরে):**
```
🤖 "সুপ্রভাত! আজকের plan:
    ✅ ফজর হয়েছে?
    📖 কোরআন: আজকের goal
    💪 আজকের ৩টা task ready"
```

**রাত (রাত ৯-১০টা):**
```
🤖 "আজকের summary:
    নামাজ: ৪/৫ ✅
    ঘুম: গতকাল ৬.৫ ঘণ্টা
    Habit streak: 🔥 5 দিন!
    Suggestion: কাল একটু তাড়াতাড়ি ঘুমাও"
```

### 7. Voice Control — "Talk to Rizers"
App এর ভেতরে পুরো voice দিয়ে control করা যাবে।

**In-App Voice (Web + Native):**
- 🎤 Mic button press → User কথা বলে → AI শুনে কাজ করে
- AI voice এ reply করে (Text-to-Speech)
- Web Speech API — সম্পূর্ণ FREE
- কোনো extra cost নেই

**Voice দিয়ে যা করা যাবে:**
```
🎤 "আজকের habit গুলো দেখাও"
🎤 "ফজর mark করো"
🎤 "আমার ঘুমের goal ৭ ঘণ্টা করো"
🎤 "নতুন habit add করো — রোজ পানি পান"
🎤 "এই সপ্তাহে কেমন ছিলাম?"
🎤 "কাল সকাল ৯টায় meeting set করো"
🎤 "সব habit delete করো"
🎤 "আমার ঘুমের analysis দাও"
```

**"Hey Rizer" Wake Word (Native App এ):**

| Platform | App Open | App বন্ধ |
|----------|----------|----------|
| Android | ✅ Full wake word | ✅ Background service |
| iPhone | ✅ Full in-app voice | ⚠️ "Hey Siri, Hey Rizer" shortcut |

- Android: সত্যিকার background wake word — Picovoice Porcupine (free tier)
- iPhone: Siri Shortcuts দিয়ে → "Hey Siri, Rizers খোলো" → AI voice mode চালু

---

## API Key Strategy — $0 Cost Plan

### Key Rotation System:
```
Request আসলো
      ↓
Cloudflare Worker:
Gemini Key 1 limit শেষ? → Key 2
Key 2 শেষ? → Key 3 ... Key 50
Gemini সব শেষ? → Groq Key 1
Groq সব শেষ? → "কাল আবার চেষ্টা করো"
      ↓
User বুঝতেই পারবে না ✅
```

### Capacity Calculation:
```
50 Gemini keys × 1,500/day = 75,000 request/day
10 Groq keys   × 1,000/day = 10,000 request/day
─────────────────────────────────────────────────
মোট:                        85,000 request/day

Heavy user  (১৫ exchange/day) → 5,666 জন
Medium user (১০ exchange/day) → 8,500 জন
Realistic mixed               → ~10,000 জন

খরচ: $0 ✅
```

---

## Native App Plan (React Native)

### কেন React Native:
- একই code → Android + iOS দুইটাই
- Wake word support ✅
- Background service ✅
- Play Store + App Store publish ✅
- Push notification (proper) ✅

### Voice Tech Stack:
```
react-native-voice     → Speech-to-Text (free)
react-native-tts       → Text-to-Speech (free)
Picovoice Porcupine    → "Hey Rizer" wake word (free tier)
Gemini API             → AI brain (already planned)
```

---

## Technical Architecture

```
FRONTEND (Rizers App)
├── Floating Widget (HTML/CSS/JS)
├── Chat Drawer UI
├── Chip/Button quick replies
├── Tool execution layer
└── localStorage sync

BACKEND (Cloudflare Worker)
├── Gemini 2.0 Flash (primary AI)
├── Groq (fallback / free tier)
├── Tool definitions
├── Credit management
└── User memory/context

DATABASE (Supabase)
├── User profile & preferences
├── Conversation history
├── 7-day history cache
└── Cross-device sync
```

---

## AI Provider — Groq (Updated Decision)
**Last Updated: 2026-07-12**

### কেন Groq (Gemini এর বদলে):
- **14,000 request/day FREE** per API key — প্রতি user এর জন্য আলাদা key
- User নিজের key নিজে পাবে → unlimited AI support
- "Rizers AI" branded — user জানবে না under the hood Groq use হচ্ছে
- Already Tayef এর অন্য system এ proven & working

### AI Provider Strategy:
```
Web Version:
  Rizers এর নিজের Groq key → Cloudflare Worker → সব user share করে
  Daily limit থাকবে (Rizers এর key এর উপর নির্ভর)

Native App Version:
  প্রতি user এর নিজস্ব Groq key → 14k request/day unlimited
  User কিছু জানে না — সব "Rizers AI" হিসেবে দেখে
```

---

## Native App — Groq Auto-Setup Flow (IMPORTANT)
**এটা native app release এর সময় implement হবে**

### User Experience:
```
১. User কিছু AI chat করে (Rizers এর key দিয়ে)
২. Popup: "Unlimited AI চালু করুন — ১ মিনিটে"
৩. User click করে → App এর ভেতরে Setup Screen খোলে
৪. "Google দিয়ে চালু করুন" button দেখায়
৫. User Google account select করে
৬. ⟳ কয়েক সেকেন্ড...
৭. ✅ "AI চালু হয়ে গেছে! এখন unlimited use করুন"
```

### Technical Flow (React Native):
```
react-native-webview দিয়ে hidden automation:

Step 1: groq.com load
Step 2: "Continue with Google" auto click (JS inject)
Step 3: User Google account select করে (real OAuth — user দেখে)
Step 4: URL detect → /dashboard → /keys এ navigate
Step 5: "Create API Key" auto click (JS inject)
Step 6: DOM থেকে key capture → postMessage() → app এ
Step 7: Supabase এ save (user এর row এ)
Step 8: WebView বন্ধ → Success screen
```

### Code Pattern (React Native):
```javascript
// onNavigationStateChange দিয়ে URL track
if (url.includes('groq.com') && !url.includes('/dashboard')) {
  webview.injectJavaScript(`
    document.querySelector('[data-provider="google"]').click()
  `)
}
if (url.includes('/dashboard')) {
  webview.injectJavaScript(`window.location='https://console.groq.com/keys'`)
}
if (url.includes('/keys')) {
  webview.injectJavaScript(`
    document.querySelector('.create-key-btn').click();
    setTimeout(() => {
      const key = document.querySelector('.key-value').textContent;
      window.ReactNativeWebView.postMessage(JSON.stringify({type:'KEY', value:key}));
    }, 2000)
  `)
}

// App side
onMessage = (e) => {
  const data = JSON.parse(e.nativeEvent.data);
  if (data.type === 'KEY') {
    saveToSupabase(data.value); // users table এ groq_key column
    showSuccess();
  }
}
```

### Branding Rules:
```
✅ দেখাবে: "Rizers AI"
✅ দেখাবে: "AI চালু হয়ে গেছে"
❌ লুকাবে: "Groq"
❌ লুকাবে: "API Key"
❌ লুকাবে: Technical কিছু
```

### Supabase Schema (users table এ add করতে হবে):
```sql
ALTER TABLE users ADD COLUMN groq_key TEXT;
ALTER TABLE users ADD COLUMN ai_setup_done BOOLEAN DEFAULT FALSE;
ALTER TABLE users ADD COLUMN ai_messages_used INTEGER DEFAULT 0;
```

---

## Build Phases

### Web App Phase (এখন):
| Phase | Feature | Status |
|-------|---------|--------|
| 1 | Floating widget UI + chat drawer | ⏳ Pending |
| 2 | Cloudflare Worker + Groq connected | ⏳ Pending |
| 3 | Daily limit system (Rizers এর key দিয়ে) | ⏳ Pending |
| 4 | Onboarding flow (questions + chips) | ⏳ Pending |
| 5 | Tool calling (full app control) | ⏳ Pending |
| 6 | Proactive "Rizers Pulse" (7-day history check) | ⏳ Pending |
| 7 | Daily summary morning + night | ⏳ Pending |
| 8 | In-app voice chat (mic button + TTS reply) | ⏳ Pending |
| 9 | Community features + social AI | ⏳ Pending |

### Native App Phase (ভবিষ্যত):
| Phase | Feature | Status |
|-------|---------|--------|
| 10 | React Native convert (Android + iOS) | 🔮 Future |
| 11 | Groq Auto-Setup Flow (WebView automation) | 🔮 Future |
| 12 | Per-user Groq key → unlimited AI | 🔮 Future |
| 13 | "Hey Rizer" wake word — Android | 🔮 Future |
| 14 | Siri Shortcuts — iPhone | 🔮 Future |
| 15 | Background AI service | 🔮 Future |
| 16 | Play Store publish | 🔮 Future |
| 17 | App Store publish | 🔮 Future |

---

## Before We Start — Needed:
1. **Groq API Key** → console.groq.com → "Create API Key" (Rizers এর নিজের account)
2. **Cloudflare Account** → cloudflare.com → Workers & Pages (free plan ok)

---

## Cost Estimate
- **Web Version: ~$0** — Groq free tier (Rizers এর নিজের key)
- **Native App: $0 per user** — প্রতি user এর নিজের Groq key (14k/day)
- Voice (Web Speech API + browser TTS): $0
- Wake word Picovoice free tier: $0
- React Native: $0 (open source framework)
- Play Store: $25 one-time fee
- App Store: $99/year
